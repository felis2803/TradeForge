# Результаты проверок manual smoke-сценариев

- **Дата/окружение:** локальный запуск, ветка `work`.
- **Запущенные проверки:** `pnpm lint`.
- **Статус:** **неуспешно** — ESLint/Prettier остановились на существующих проблемах форматирования в UI и SDK компонентах.

## Детали

- `pnpm lint` завершился с ошибками форматирования (`prettier/prettier`) в `apps/manual-trading/src/ManualTrading.tsx`, `apps/web/src/pages/ManualTrading.tsx` и `packages/sdk/dashboard/src/components/PriceChart.tsx`.
- Ошибки относятся к текущему состоянию кода и не были изменены в рамках проверки. Исправление потребует автоматического форматирования (например, `pnpm format` или `eslint --fix`).

## Следующие шаги

1. Применить автоформатирование к указанным файлам и повторно выполнить `pnpm lint`.
2. Дополнительно при необходимости запустить профильные тесты (`pnpm test`, `pnpm typecheck`) после исправления форматирования.
